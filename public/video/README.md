# Hero video assets

- `4k-hero-banner.webm` — hero background (desktop + mobile)
- `hero-mobile.webm` — optional smaller mobile encode; add and wire up in `HeroSection.tsx` if needed

The hero overlay gradient still shows if the video fails to load.
